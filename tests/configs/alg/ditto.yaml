hyperparameters:
  client:
    batch_size: 10
    lam: 1
    local_epochs: 10
    loss: CrossEntropyLoss
    model: MNIST_2NN
    optimizer:
      lr: 0.01
    scheduler:
      gamma: 1
      step_size: 1
    tau: 5
  model: MNIST_2NN
  server:
    weighted: true
name: fluke.algorithms.ditto.Ditto
