hyperparameters:
  client:
    batch_size: 10
    local_epochs: 5
    loss: CrossEntropyLoss
    eps: 0.0314
    k_prop: 1.0
    optimizer:
      lr: 0.1
      # momentum: 0.9
      # weight_decay: 0.0001
    scheduler:
      gamma: 1
      step_size: 1
  server:
    weighted: true
  model: MNIST_2NN
name: fluke.algorithms.fat.FAT
