hyperparameters:
  client:
    batch_size: 8
    lam: 1
    local_epochs: 1
    loss: CrossEntropyLoss
    model: MNIST_CNN
    n_protos: 10
    optimizer:
      lr: 0.01
      momentum: 0.5
      # weight_decay: 0.0001
    scheduler:
      gamma: 1
      step_size: 1
  model: MNIST_CNN
  server:
    n_protos: 10
    weighted: true
name: fl_bench.algorithms.fedproto.FedProto
